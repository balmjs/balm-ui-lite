## Helpers

- `getType(any)`
- `isBoolean(any)`
- `isNull(any)`
- `isUndefined(any)`
- `isNumber(any)`
- `isString(any)`
- `isSymbol(any)`
- `isObject(any)`
- `isArray(any)`
- `isFunction(any)`
- `detectIE()`

---

__For 🌰__

```js
import { helpers } from 'balm-ui-lite';

let msg = 'Hello BalmUI';
let result = helpers.isNumber(msg);
```
